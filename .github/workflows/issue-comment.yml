name: funny comment
on:
  issues:
    types: [opened]
jobs:
  comment:
    if: github.event_name == 'issues'
    runs-on: ubuntu-latest
    steps:
        -name: Add funny comment
         with: 
          GIPHY_KEY: ${{ secrets.GIPHY_KEY }}
          comment_type: 'gif'
